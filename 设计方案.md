# 凯凯搜题助手 · 手机版在线搜题系统设计方案

> 版本：v0.2（针对本题库优化版）
> 日期：2026-09-06
> 项目路径：`D:\Trae项目\凯凯搜题助手`
> 当前状态：方案优化稿，优先优化搜索/模糊搜索/搜索速度

---

## 0. 本版核心优化方向

本版专门针对 `交安复习题全部.docx` 这一份真实题库做了优化，核心原则是：

> **一切为搜索服务**：解析结果直接面向“搜索质量”和“搜索速度”设计，而不是只做一个题库展示。

本版重点优化：

1. 把文档中实际存在的**案例分析题、背景资料、知识卡片、重复章节、缺失选项标签、无空格选项**等真实结构纳入解析设计。
2. 建立**多路搜索索引**：普通全文 + 拼音全拼 + 拼音首字母 + 子串/模糊匹配。
3. 设计**搜索排序**：题干 > 选项 > 答案 > 章节/材料，并支持模糊命中加权。
4. **默认形态：手机能直接打开**。采用静态 PWA / 单文件版，生成后可放任意静态托管或局域网，手机浏览器直接访问，无需安装 App、无需后端。
5. 在线服务版只作为可选进阶方案，不阻塞“手机直接打开”的第一版。

### 0.1 对齐原则（最高优先级，不可妥协）

**核心红线：任何生成结果都必须和原始题库对齐，尤其是答案。绝不“猜答案”，绝不“自动改正”答案。**

1. **源头即真相**
   - 所有题目、选项、答案、备注、案例背景都来自 `交安复习题全部.docx` 原始段落；
   - 每道题保存 `sourceParagraphStart/End` 和 `rawText`，方便随时回查原文档；
   - 不修改、不覆盖、不删除原文档。

2. **答案三态，禁止猜测**
   - `extracted`：从源文档明确提取（如 `正确答案:D`、`（C）`、`（√）`）；
   - `derived`：从题干括号、`√×` 等推导得出，必须标记来源并人工复核；
   - `missing`：源文档没有答案，或格式无法识别，**必须标记为缺失**，不得用选项顺序、常见答案、AI 猜测来补答案。

3. **答案必须可溯源**
   - 每个答案字段记录：
     ```json
     {
       "answerRaw": "正确答案:D",
       "answerSource": "answer_line",
       "answerConfidence": "high",
       "sourceLine": 1234
     }
     ```
   - `answerSource` 取值：
     - `answer_line`：`正确答案:…` / `答案:…`
     - `inline_paren`：题干内 `（C）` / `（√）` / `（×）`
     - `judge_symbol`：`√` / `×`
     - `option_mapping`：判断题 `A.正确 B.错误` + `正确答案:B`
   - 任何答案入库前必须能回指原始段落。

4. **发现冲突时不许静默选一边**
   - 题干内答案是 `C`，但 `正确答案` 行是 `D` → 标记 `answer_conflict`，保留两个原始值，等待人工确认；
   - 答案是 `E`，但选项只有 A/B/C/D → 标记 `answer_out_of_options`；
   - 多选答案出现重复字母、全角/半角混用、`A，C，E` 等，先归一化再校验，但原始值保留在 `answerRaw`。

5. **入库前自动校验**
   - 答案字母必须是选项 key 的子集；
   - 判断题答案只能是 `正确/错误/A/B/√/×`；
   - 单选题答案只能是一个；
   - 多选答案至少一个字母；
   - 校验失败 → 进入 `review` 状态，不进入正式题库。

6. **人工确认后门**
   - 导入预览页必须能看到：正常题、待确认题、缺失答案题、冲突题；
   - 只有 `extracted`/`derived` 且 `answerConfidence=high` 的题可直接入库；
   - `missing` 或 `conflict` 必须人工处理，可“确认保留”、可“手工补录”，但**绝不自动生成**。

7. **发布前 golden test**
   - 从原文档中人工抽取 **200 道**作为基准题（即 golden set）；
   - 每次解析器改动后跑 golden set，答案一致率必须 **100%**，题目文本一致率不低于 **99%**；
   - 否则不允许生成正式题库。

8. **搜索不得篡改答案**
   - 搜索索引只服务“检索”，不参与答案生成；
   - 答案展示、复制一律使用题库中已校验的字段；
   - 重复题折叠时，展示的答案必须来自同一条源记录，不能混用不同来源的答案。

---

## 1. 项目背景

用户拥有一份 Word 题库文件：

```
D:\Trae项目\凯凯搜题助手\交安复习题全部.docx
```

该文档包含大量公路/交通安全相关复习题，格式不统一。实际核查结果（基于只读文本提取）：

- 非空段落约 **5137** 段。
- 普通单选/多选/判断题数量约 **1000~1500** 道（最终以解析器统计为准）。
- 包含 **案例分析题**：至少 5 个“案例分析题”标题、9 处“背景资料”、29 个 `（N）问:` 子题、46 个 `①②③④` 子题。
- 包含 **知识卡片/法规条款**：如工亡补助金、事故等级、法规条款列表等，这些不是题目，需要单独归类。
- 存在大量**重复章节**：`一、单选题`、`二、多选题`、`三、判断题` 等在不同位置重复出现，同一道题可能重复多份。

本方案要解决的问题：

1. 把该文档自动解析为高质量的结构化题库；
2. 提供手机端友好的搜索界面；
3. **重点保证“模糊搜索”和“搜索速度”**，让用户输入少量关键字、拼音首字母、错字也能快速找到题；
4. 后续可扩展：错题本、收藏、随机练习、导出题目。

---

## 2. 需求目标

### 2.1 核心目标（按优先级）

1. **搜得到**：普通关键字、拼音首字母、全拼、题号、答案都能搜到。
2. **搜得快**：输入防抖 200~300ms，结果 200ms 内返回；列表懒加载/虚拟滚动；静态版不卡顿。
3. **搜得准**：题干命中优先，选项、答案、章节/材料依次降权。
4. **手机直接打开**：生成静态页面后，手机浏览器直接访问 URL 即可使用，不需要安装 App、不需要登录、不需要后端。
5. **能维护**：把 Word 重新导入后自动重建索引，不需要改代码。

### 2.2 非目标（当前版本不做）

- 不做复杂账号体系、付费系统、后台管理权限。
- 不做在线编辑题目（可先通过重新导入更新题库）。
- 不做高并发大规模多人考试系统，重点满足个人/小团队搜题需求。

---

## 3. 用户场景

| 场景 | 用户操作 | 系统响应 |
| --- | --- | --- |
| 日常搜题 | 输入关键词、拼音首字母 | 毫秒级返回匹配题目，支持模糊容错 |
| 按题型复习 | 点击“多选题 / 判断题”Tab | 展示对应题型题目 |
| 按章节复习 | 点击“综合知识和能力 / 法律体系” | 展示对应章节题目 |
| 查看答案 | 点击题目或直接看答案卡片 | 显示答案，可复制完整答案 |
| 案例分析 | 输入“隧道”“事故等级”等 | 能搜到案例背景及其子题 |
| 知识卡片 | 输入“工亡补助金” | 能搜到知识卡片，不误入题目列表 |
| 更新题库 | 上传新的 Word 文档 | 自动解析、预览、生成题库和搜索索引 |

---

## 4. 功能需求

### 4.1 前端功能（搜索优先）

1. **搜索栏**
   - 支持关键字输入；
   - 支持回车搜索；
   - 支持防抖自动搜索；
   - 支持清空；
   - 支持搜索建议/热搜词；
   - 支持“拼音首字母”输入提示（例如 `aqsc` → 安全生产）。

2. **题型筛选**
   - 全部；
   - 单选；
   - 多选；
   - 判断；
   - 案例分析（子题单独显示，可关联到背景资料）；
   - 填空（若题库中有）；
   - 知识卡片（可单独 Tab 展示）。

3. **章节筛选**
   - 根据文档实际章节动态生成；
   - 支持“知识章节”和“题型章节”两种维度。

4. **题目列表**
   - 显示题型标签；
   - 显示题号/题干；
   - 显示选项（可折叠）；
   - 显示答案卡片；
   - 显示解析/备注（如有）；
   - 命中关键词高亮。

5. **题目详情**
   - 点击题目可展开详情；
   - 展示完整题干、选项、答案、备注；
   - 如果是案例子题，展示所属案例背景资料；
   - 提供“复制完整答案”（含选项文本）。

6. **导入/管理页（可选，后台）**
   - 上传 Word；
   - 解析预览；
   - 展示解析成功率；
   - 对未识别题目进行人工修正或跳过；
   - 一键生成题库和搜索索引。

### 4.2 后端功能

1. **文档解析**：读取 `.docx` 并提取正文。
2. **题目识别**：识别章节、题型、题干、选项、答案、备注、案例背景、知识卡片。
3. **题库存储**：写入 SQLite / JSON 文件。
4. **搜索索引构建**：生成全文索引、拼音索引、首字母索引、子串/模糊索引。
5. **搜索接口**：按关键字、模糊词、拼音、题型、章节、题号查询。
6. **统计接口**：题目总数、各题型数量、各章节数量、最近更新时间。

---

## 5. 文档现状分析（本题库实测）

已使用只读方式分析 `交安复习题全部.docx`，实际发现以下结构。

### 5.1 普通题目排版

**早期章节**：答案直接写在题干括号内，选项可能分散多行，甚至选项和下一题号同行：

```
1、下列情况中，针对( C )应当采用地质雷达配合破检方法检查……
A.副董事长   B.总经理   C.实际控制人   D.技术负责人 3、2002 年开始……
A.6B.5C.1D.8
```

**中后段章节**：题干留空，答案单独一行：

```
1、根据《公路工程施工安全技术规范》规定，不属于危险性较大工程的是（  ）。
A.桥梁施工水上作业平台
B.开挖深度为5m的基坑
C.桥梁悬臂浇筑节段长度不大于2m
D.Ⅱ级围岩地段隧道
正确答案:D
```

**多选答案**：

```
正确答案:A,B,C,E
正确答案:A，C，E
```

**判断题**：

```
1、跨径小于或等于140m的梁式桥，斜拉桥，悬索桥需要进行安全风险评估。（  ）
A.正确  B.错误
正确答案: B
```

也有部分判断题不显示 A/B 选项，直接：

```
1、跨线施工安全防护棚应设置轮廓灯、警示灯、爆闪灯等设施（  ）。
正确答案:正确
1、……（√）
1、……（×）
```

### 5.2 案例分析题（重点补充）

文档中存在多个案例分析题区块：

```
（四）案例分析题
第1题:
背景资料：某公路桥梁工程……
（1）问:依据《生产安全事故报告和调查处理条例》安全生产事故等级划分，本次事故属于（ ）。
A.一般事故
B.较大事故
C.重大事故
D.特别重大事故
正确答案: A
（2）问:……
```

还有这些形式：

```
第1题印刷资料
第六章 隧道工程案例分析题：
背景资料:2012年5月19日……
（1）问:……
```

以及用 `①②③④` 作为子题编号的案例分析题：

```
背景资料：……
① 2009 年事故死亡赔偿每人 40 万元符合当年的赔偿标准(√)。
② 从 2015 年开始，工伤事故死亡赔偿标准大大提高(×)。
③ 此起安全事故的等级属于(B)。
A.一般事故B.较大事故C.重大事故D.特别重大事故
```

**解析策略**：案例分析题是一个“题组”，背景资料 + 多个子题；每个子题仍可独立搜索，但搜索结果显示“所属案例”上下文。

### 5.3 知识卡片/法规条款

这些不是普通题目，不能进入题目搜索主列表：

```
2016 年工伤死亡赔偿标准：
一、一次性工亡补助金标准为:2015 年度全国城镇居民人均可支配收入 31195 元……
二、丧葬补助金标准:……
三、供养亲属抚恤金标准如下
```

```
《生产安全事故报告和调查处理条例》
生产安全事故等级：
一、特别重大事故，是指造成30人以上死亡……
二、重大事故，是指……
```

```
(1)支架高度不小于 5m;跨度不小于 10m;(2)搭设高度 24m 及以上的落地式钢管脚手架工程;……
```

**解析策略**：标记为 `knowledge` 类型，可搜索，但显示为“知识卡片”，不与题目混排。

### 5.4 缺失选项标签与无空格选项

实际文档大量出现：

```
设计文件批复及专项施工方案编制不到位 B.施工材料及产品以次充好
A.6B.5C.1D.8
立即撤销生产经营单位负责人的职权 C.立即赶到事故现场
生病治疗住院 B.毁灭有关证据 D.报告现场情况
```

必须用“当前题目选项序列”状态机自动补缺失标签，并按 `(?=[A-H][、.．])` 无空格切分。

### 5.5 重复章节

文档中重复出现：

- `一、单选题`（第 1 行、第 3981 行）
- `二、多选题`（第 23 行、第 3998 行）
- `三、判断题`（第 4009 行）
- `二、单选题`（第 2252 行）

同题可能在不同位置重复。**搜索时默认只保留一条最优结果，其余作为“重复来源”折叠显示**。

### 5.6 其他格式

- 题干跨多个段落（如“安全生产月”题目、案例背景资料）。
- 部分题号缺失（例如某题干行没有编号，需要按上下文补编号）。
- 答案行包含备注/知识点：
  ```
  正确答案:错误（注：每次开挖进尺不得超过一榀钢架间距）
  正确答案:错误（知识点：各工作面应独立通风）
  ```
- 章节标题存在 `第六章 隧道工程判断题：`、`（四）综合知识和能力` 等变体。

---

## 6. 总体架构

### 6.1 逻辑架构

```
┌─────────────────────────────┐
│       手机浏览器 / PWA        │
│       Vue3 + Vant UI         │
│       Web Worker 搜索索引     │
└──────────────┬──────────────┘
               │ HTTP / JSON（在线版）
┌──────────────▼──────────────┐
│        后端 API 服务          │
│   FastAPI + SQLite + FTS5    │
│   拼音/首字母索引、模糊索引    │
└──────────────┬──────────────┘
               │
┌──────────────▼──────────────┐
│       题库数据存储            │
│   questions.db / JSON        │
└──────────────┬──────────────┘
               │
┌──────────────▼──────────────┐
│         Word 导入器          │
│  docx 解析 → 规则识别 → 清洗  │
│  → 建搜索索引 → 模糊词/拼音    │
└─────────────────────────────┘
```

### 6.2 搜索索引架构（核心）

无论在线版还是静态版，索引都应包含以下四层：

1. **全文索引**
   - 题干、选项、答案、备注、章节、案例背景、知识卡片正文。
2. **拼音索引**
   - 每个可搜索字段的全拼（如 `anquanshengchan`）。
3. **拼音首字母索引**
   - 每个可搜索字段的首字母（如 `aqsc`）。
4. **子串/模糊索引**
   - 中文按相邻字/词建 n-gram，用于处理“用户少字、多字、错字”的模糊匹配。

### 6.3 推荐落地形态：手机直接打开

第一版优先做**静态 PWA / 单文件版**，目的是：

- 手机浏览器直接打开 URL 就能用，不需要安装 App；
- 不需要后端服务器，不依赖账号登录；
- 搜索、题目、答案全部在手机浏览器本地完成；
- 生成物为静态文件，可放任意空间。

静态版生成物：

```
dist/
├── index.html          # 手机端主页面（含 UI 和搜索逻辑）
├── questions.json      # 结构化题库
├── search-index.json   # 搜索索引（全文/拼音/首字母/n-gram）
├── manifest.json       # PWA 清单
├── sw.js               # Service Worker（离线缓存）
└── icons/              # 应用图标
```

如果你的手机和电脑在同一个 WiFi，还可以用最简单的方式直接打开：

```bash
# 在项目目录启动本地静态服务器
python -m http.server 8000
# 手机浏览器访问
http://电脑局域网IP:8000
```

- 单文件版：如果题目量不大，也可以把 `questions.json` 和 `search-index.json` 内嵌进 `index.html`，生成一个“单文件 HTML”。
- 单文件版适合：直接发给别人、拷贝到手机、用任意静态服务器打开。
- 注意：iOS Safari 直接打开本地 `file://` 文件体验受限，建议使用静态托管或局域网 HTTP 方式；Android 浏览器也可用局域网 HTTP 方式获得更好体验。

### 6.4 两种手机交付方式（都可行）

**方式 A：GitHub Pages / 任意静态托管（先做，最快）**

- 把 `dist/` 上传到 GitHub 仓库，开启 GitHub Pages；
- 手机浏览器直接访问 `https://用户名.github.io/仓库名/`；
- 不需要后端、不需要安装；
- 支持 PWA，可“添加到主屏幕”。

**方式 B：生成 Android APK（进阶，离线安装）**

- 用同一套静态页面，通过 **Capacitor / WebView** 打包成 Android APK；
- 可安装到手机桌面，像普通 App 一样离线使用；
- 不需要应用商店，直接安装 APK；
- 适合长期自用、发给朋友、不想每次访问网页。

两种方式共用同一份 `dist/`，先做 A，再做 B 不冲突。

---

## 7. 数据模型

### 7.1 题目对象

```json
{
  "id": "q_000001",
  "type": "single",
  "typeName": "单选题",
  "chapter": "综合知识和能力",
  "section": "单选题",
  "sourceNo": "1",
  "sourceSections": ["一、单选题", "一、单选题"],
  "sourceParagraphStart": 1234,
  "sourceParagraphEnd": 1240,
  "rawQuestionLines": [1234, 1235, 1236],
  "rawText": "原始段落文本",
  "question": "宜在（ ）之内完成碾压成型。",
  "options": [
    { "key": "A", "text": "1h" },
    { "key": "B", "text": "1.5h" },
    { "key": "C", "text": "2h" },
    { "key": "D", "text": "2.5h" }
  ],
  "answer": ["C"],
  "answerText": "C",
  "answerRaw": "正确答案:C",
  "answerSource": "answer_line",
  "answerConfidence": "high",
  "verificationStatus": "verified",
  "note": "",
  "caseId": "case_001",
  "materialText": "",
  "dupGroupId": "dup_0001"
}
```

### 7.2 案例题组对象

```json
{
  "id": "case_001",
  "title": "第1题",
  "type": "case",
  "chapter": "案例分析题",
  "materialText": "背景资料：某公路桥梁工程……",
  "subQuestionIds": ["q_000123", "q_000124"],
  "sourceParagraphStart": 2811,
  "sourceParagraphEnd": 2836
}
```

### 7.3 知识卡片对象

```json
{
  "id": "k_0001",
  "type": "knowledge",
  "chapter": "知识卡片",
  "title": "一次性工亡补助金标准",
  "content": "2015 年度全国城镇居民人均可支配收入 31195 元……",
  "sourceParagraphStart": 427,
  "sourceParagraphEnd": 429
}
```

### 7.4 搜索索引字段（核心）

每道题/卡片生成一个 `search_doc`：

```json
{
  "id": "q_000001",
  "type": "single",
  "text": "宜在（ ）之内完成碾压成型。 1h 1.5h 2h 2.5h C 单选题 综合知识和能力",
  "question": "宜在（ ）之内完成碾压成型。",
  "optionsText": "1h 1.5h 2h 2.5h",
  "answerText": "C",
  "note": "",
  "chapter": "综合知识和能力",
  "materialText": "",
  "pinyin": "yizaizhineiwanchengnianyachengxing",
  "pinyinAbbr": "yzzznwcn ycx",
  "grams": ["宜在", "在（", "（）", "）之", "之内", "内完", ...]
}
```

### 7.5 题型枚举

| type | typeName | 说明 |
| --- | --- | --- |
| `single` | 单选题 | 答案通常为单个字母 |
| `multiple` | 多选题 | 答案为一个或多个字母 |
| `judge` | 判断题 | 答案可以是“正确/错误”或 A/B |
| `fill` | 填空题 | 答案为一个或多个短语 |
| `case` | 案例分析题 | 一个题组，包含背景资料和多个子题 |
| `knowledge` | 知识卡片 | 法规/知识点/案例背景，不参与题目列表 |

---

## 8. 题库导入解析流程

### 8.1 总体流程

```
读取 .docx
  → 提取段落文本（保留段落索引）
  → 识别章节/题型标题
  → 识别案例分析题组和背景资料
  → 识别知识卡片
  → 识别题目起点（含第N题、（N）问、①②）
  → 识别选项（含补标签、无空格切分）
  → 识别答案（含√×、知识点/注）
  → 清洗题目文本（全半角、空格、括号归一化）
  → 去重（规范化后去重）
  → 生成结构化题目 + 案例题组 + 知识卡片
  → 生成解析报告
  → 一致性校验（答案/选项/原文对齐）
  → 人工预览/修正
  → 写入题库（仅通过校验的题目）
  → 构建搜索索引（全文/拼音/首字母/n-gram）
  → 再次回读校验（与源文档抽样比对）
```

### 8.2 提取文本

`.docx` 本质是 ZIP 包，正文在 `word/document.xml` 中。按段落 `<w:p>` 提取 `<w:t>` 文本，并保留段落序号。

### 8.3 章节/标题识别（增强）

要识别：

```
一、 单选题
二、多选题
三、判断题
（四）综合知识和能力
二、单选题
第六章 隧道工程判断题：
（四）案例分析题
```

**标题判定规则**：

- 以中文序号开头；
- 或 `（N）` 开头；
- 或 `第X章` 开头；
- 且满足以下至少一条：
  - 包含“单选题/多选题/判断题/案例分析/综合知识/法律/隧道工程”等关键词；
  - 或者行短（≤30字）且下一行是题目起点/选项/答案/背景资料。
- 否则不当作章节标题，例如：
  ```
  一、一次性工亡补助金标准为:……
  二、丧葬补助金标准:……
  一、特别重大事故，是指……
  ```

### 8.4 题目起点识别（增强）

支持以下格式：

```
1、
1.
1题：
1题:
第1题:
第1题印刷资料
（1）问:
①
②
```

正则示例：

```
^\d+[、.．]\s*
^\d+题[:：]?\s*
^第\d+题[:：]?\s*
^[（(]\d+[)）]\s*问[:：]?\s*
^[①②③④⑤⑥⑦⑧⑨⑩]
```

**注意**：不是所有 `(1)` `(2)` 都是题目，如法规条款列表 `(1)支架高度不小于5m……` 是知识卡片内容，需要靠后续是否出现选项/答案来判定。

### 8.5 选项识别（增强）

支持格式：

```
A.xxx
A、xxx
B. xxx
C.xxx
A.4.5  B.5.5  C.6.5  D.7.5
A.6B.5C.1D.8
```

处理规则：

1. 按 `(?=[A-H][、.．])` 切分，兼容无空格。
2. 对缺失标签的选项，按当前题目已出现的选项序列自动补 A/B/C/D。
3. 选项文本归一化：全角转半角、去首尾空格、合并内部多余空格。
4. 把“A.正确 B.错误”作为判断题选项。

### 8.6 答案识别（增强）

支持：

```
正确答案:D
正确答案：A,B,C
正确答案:A，C，E
正确答案:正确
正确答案:错误
正确答案: A
答案：C
（ C ）
（√）
（×）
（ ） → 后续正确答案
```

多选答案处理：

```
A,B,C,E  →  ["A","B","C","E"]
A，C，E   →  ["A","C","E"]
```

判断题答案处理：

```
正确  →  ["正确"]
错误  →  ["错误"]
B     →  ["B"]
√     →  ["正确"]
×     →  ["错误"]
```

备注/知识点处理：

```
正确答案:C（注：加工棚宜采用轻型钢结构）
→ answer = ["C"]
→ note = "注：加工棚宜采用轻型钢结构"

正确答案:错误（知识点：各工作面应独立通风）
→ answer = ["错误"]
→ note = "知识点：各工作面应独立通风"
```

### 8.7 脏数据切分

当选项与下一题号出现在同一段时：

```
D.技术负责人 3、依据法律规定……
```

需要按题目起点正则切分为：

```
D.技术负责人
3、依据法律规定……
```

### 8.8 题干跨段合并

很多题目题干被 Word 拆成多个段落：

```
3、法律规定施工单位负责人接到事故报告后，应当迅速组织抢救，
减少人员伤亡，防止事故扩大，妥善保护现场，不得( AB )。
```

合并规则：

- 当前题目尚未出现“选项/答案/下一题号”时，下一段文本并入题干；
- 案例背景资料按“背景资料”标记整体合并；
- 必须记录 `sourceParagraphStart/End`。

### 8.9 去重策略（搜索友好）

1. 先做规范化：全半角、空格、括号、答案符号（√/×/正确/错误）、选项字母尺寸。
2. **题干规范化后完全相同**视为重复题。
3. 搜索时默认只返回一条“最优题目”，其余重复来源折叠显示在详情页。
4. 题干相似但选项不同，不自动合并，交由人工确认。

### 8.10 答案一致性校验（必须）

每题生成后必须通过以下校验，否则不进入正式题库：

1. **答案来源校验**
   - 每条答案必须记录 `answerRaw`、`answerSource`、`answerConfidence`；
   - 不满足“可溯源”的答案标记为 `missing` 或 `review`。

2. **答案与选项对齐**
   - 单选题/多选题答案字母必须是选项 key 的子集；
   - 判断题答案只能是 `正确/错误`、`A/B` 或 `√/×`；
   - 多选答案至少一个字母；
   - 校验失败 → 进入 review，不自动修改。

3. **题干内嵌答案与答案行冲突**
   - 例如题干是 `( C )`，答案是 `正确答案:D`；
   - 不自动选一个，标记 `answer_conflict`，保留两个原始值，人工确认。

4. **原答案保留**
   - 所有归一化（全半角、空格、逗号）只作用于搜索和显示；
   - `answerRaw` 永远保留原始字符串，用于人工核对。

5. **抽样回读**
   - 入库后随机抽取 ≥ 100 道，回到原 docx 对应段落人工/半自动比对；
   - 答案一致率目标 **100%**；不一致必须修复后才能发布。

### 8.11 解析报告

```json
{
  "totalParagraphs": 5137,
  "recognizedQuestions": 1280,
  "caseGroups": 9,
  "caseSubQuestions": 75,
  "knowledgeCards": 12,
  "single": 680,
  "multiple": 320,
  "judge": 280,
  "fill": 0,
  "unrecognizedParagraphs": 42,
  "missingAnswer": 18,
  "answerConflict": 3,
  "answerOutOfOptions": 2,
  "missingOptionLabelFixed": 130,
  "duplicateQuestions": 35,
  "answerConsistency": {
    "verifiedAnswers": 1259,
    "reviewAnswers": 21,
    "missingAnswers": 18,
    "sampleChecked": 120,
    "sampleAnswerMatchRate": 1.0
  },
  "warnings": [
    "第 1035 段疑似选项混入下一题，已自动拆分",
    "第 2247 段存在重复章节标题，已合并",
    "第 427 段识别为知识卡片，未计入题目",
    "第 560 段答案与题干内嵌答案不一致，已进入人工确认"
  ]
}
```

---

## 9. 搜索设计（核心章节）

### 9.1 搜索目标

用户输入任何以下内容，都应尽可能快速定位到题目：

1. 题干关键词，如 `安全风险评估`；
2. 选项关键词，如 `施工便道`；
3. 答案，如 `正确`、`B`、`AB`；
4. 章节名，如 `综合知识和能力`；
5. 题号，如 `78`、`第78题`；
6. 拼音首字母，如 `aqsc`；
7. 全拼，如 `anquan`；
8. 错字/少字/多字，如 `安去`、`安全评估`；
9. 案例/背景资料关键词，如 `株洲高架桥`。

### 9.2 搜索层次

按以下顺序召回和排序：

1. **前缀/精确**：输入词与题干、选项、章节、答案完全匹配或前缀匹配。
2. **全文包含**：题干/选项/答案/材料中包含输入词。
3. **中文分词匹配**：用 jieba 或 n-gram 对题目分词，任意词命中即可。
4. **拼音全拼匹配**：输入英文全拼匹配中文转拼音。
5. **拼音首字母匹配**：输入首字母（如 `aqsc`）命中。
6. **模糊匹配**：编辑距离、n-gram 相似度，用于容错。

### 9.3 在线服务版搜索实现

#### 9.3.1 SQLite FTS5

建表：

```sql
CREATE VIRTUAL TABLE questions_fts USING fts5(
  question,
  optionsText,
  answerText,
  note,
  chapter,
  materialText,
  pinyin,
  pinyinAbbr,
  grams,
  content='questions',
  content_rowid='rowid',
  tokenize='unicode61'
);
```

说明：

- 如果 SQLite 版本 >= 3.34，可使用 `trigram` tokenizer 做中文子串搜索：
  ```sql
  tokenize='trigram'
  ```
- 如果 SQLite 版本不支持，导入时用 jieba 分词后写入 `grams` 字段，查询时同样分词。
- 拼音字段直接作为普通列参与 FTS5 索引，或在查询时用 `LIKE`/`substr` 配合。

查询示例：

```sql
SELECT q.*, bm25(questions_fts, 10.0, 5.0, 1.0, 2.0, 0.5, 1.0, 1.0) AS rank
FROM questions_fts f
JOIN questions q ON q.rowid = f.rowid
WHERE questions_fts MATCH ?
ORDER BY rank
LIMIT 50;
```

#### 9.3.2 召回与排序

- 多路召回：
  - `MATCH` FTS5
  - 拼音首字母 `LIKE 'aqsc%'`
  - 全拼 `LIKE '%anquan%'`
  - 题号 `LIKE '78%'`
  - n-gram 模糊
- 排序权重：
  - 题干命中 10
  - 选项命中 5
  - 答案命中 2
  - 备注/知识点 1
  - 章节/材料 0.5
- 同权重按题目原始顺序，保证稳定。

### 9.4 静态 PWA 版搜索实现

如果采用静态版，建议使用 `MiniSearch` 或 `FlexSearch`，而不是只用 Fuse.js：

- 导入时生成一个预计算索引文件 `search-index.json`：
  ```json
  {
    "docs": [...],
    "fields": { "text": 1, "pinyin": 1, "pinyinAbbr": 1 },
    "grams": [...]
  }
  ```
- 用 Web Worker 执行搜索，避免主线程卡顿。
- 搜索索引约 5000 题、每条约 200 字，压缩后一般 < 2MB，适合 PWA 本地缓存。
- 浏览器端可配合 `IndexedDB` 缓存索引，启动后秒开。

### 9.5 模糊搜索增强

1. **全角/半角归一化**：`Ａ` → `A`，`（` → `(`，`，` → `,`。
2. **空格归一化**：去掉题干内部多余空格，`安 全 生 产` 归一为 `安全生产`。
3. **拼音首字母**：`aqsc` → `安全生产`。
4. **全拼**：`anquan` → `安全`。
5. **同义词/别名表**：可维护常见法规简称，例如“安全生产法”也可通过“安法”命中。
6. **编辑距离容错**：对 2~4 字的词允许 1 个字符差异，例如 `安去` → `安全`。
7. **n-gram**：中文按 2-gram 索引，用户输入 1~2 字也能命中最相关题目。

### 9.6 搜索速度优化

- 输入防抖 200~300ms。
- 请求取消：使用 AbortController，只保留最后一次请求结果。
- 在线版返回列表时**只返回 ID + 摘要 + 命中高亮**，点击详情再拉完整题目。
- 前端虚拟滚动，不做全量 DOM 渲染。
- 静态版用 Web Worker + 预计算索引，不在主线程搜索。
- 题目按章节/题型分块，可增量加载。
- 热门搜索词：可从题库高频词自动生成，常驻搜索页。

---

## 10. 前端界面设计

### 10.1 首页

```
┌─────────────────────────┐
│   [ 请输入试题内容/拼音 ] 🔍 │
├─────────────────────────┤
│ 全部  单选  多选  判断  案例 │
│ 综合知识  法律法规  隧道 …   │
├─────────────────────────┤
│ [单选] 1、宜在（ ）内完成  │
│ A.1h  B.1.5h  C.2h  D.2.5h│
│ 答案：C              [复制] │
├─────────────────────────┤
│ [多选] 2、下列属于……        │
│ 答案：A、B、D         [复制]│
├─────────────────────────┤
│ [案例] 3、某桥梁工程…       │
│ 查看背景资料           [复制]│
└─────────────────────────┘
```

### 10.2 搜索交互

- 输入时 200ms 防抖，显示搜索建议；
- 支持拼音首字母提示；
- 结果列表高亮命中词；
- 支持“仅看有答案/含案例/知识卡片”等筛选；
- 空结果时提示“未找到，试试拼音首字母或更短关键词”。

### 10.3 移动端适配

- 使用 Vue3 + Vant UI；
- 底部 Tab 可选：搜题、收藏、我的；
- 支持 PWA 安装到手机桌面；
- 适配 360px ~ 430px 常见手机宽度。

---

## 11. 技术选型

### 11.1 推荐首选：静态 PWA 版（手机直接打开）

| 模块 | 技术 |
| --- | --- |
| 题库生成 | Python 脚本读取 docx，生成 questions.json + search-index.json |
| 前端 | Vue 单文件 / 原生 HTML + JS（可打包成单文件 HTML） |
| 搜索 | MiniSearch / FlexSearch（Web Worker） |
| 拼音 | pypinyin |
| 模糊 | n-gram + 编辑距离 + 拼音首字母 |
| 部署 | GitHub Pages / 任意静态托管 / 局域网 HTTP / 单文件 HTML |
| 离线 | Service Worker + PWA |
| Android APK（可选） | Capacitor 打包同一份静态页面 |

这个方案的核心优势：

- **手机浏览器直接打开 URL 就能用**；
- 不需要后端服务器、不需要登录；
- 5000 题规模下，索引压缩后约 1~2MB，手机完全可承受；
- 更新题库 = 重新跑一次 Python 脚本，生成新 JSON 后覆盖静态文件。

### 11.2 可选进阶：在线服务版

如果后续需要“网页上传 Word、多人实时更新、后台管理”，再升级为：

| 模块 | 技术 |
| --- | --- |
| 后端 | Python 3.13 + FastAPI |
| 数据库 | SQLite + FTS5（可用 trigram） |
| 拼音 | pypinyin |
| 中文分词 | jieba（可选） |
| Word 解析 | python-docx / 直接解析 XML |
| 前端 | Vue 3 + Vant 4 |
| 构建 | Vite |
| 搜索 | FTS5 + 拼音/首字母/n-gram |
| 部署 | Docker / 云服务器 / 局域网 |

在线版可作为后续增强，不影响第一版“手机直接打开”的目标。

---

## 12. API 接口设计（在线版）

### 12.1 搜索题目

```
GET /api/questions/search
  ?q=安全风险评估
  &type=single
  &chapter=综合知识和能力
  &mode=fuzzy|pinyin|exact
  &page=1
  &size=20
```

响应：

```json
{
  "total": 30,
  "page": 1,
  "size": 20,
  "elapsedMs": 12,
  "items": [
    {
      "id": "q_000001",
      "type": "single",
      "typeName": "单选题",
      "question": "宜在（ ）之内完成碾压成型。",
      "options": [
        {"key": "A", "text": "1h"},
        {"key": "B", "text": "1.5h"},
        {"key": "C", "text": "2h"},
        {"key": "D", "text": "2.5h"}
      ],
      "answerText": "C",
      "note": "",
      "snippet": "……<mark>碾压</mark>成型……",
      "duplicateOf": null
    }
  ]
}
```

### 12.2 搜索建议

```
GET /api/questions/suggest?q=aqsc
```

返回：

```json
{
  "suggestions": [
    {"text": "安全生产", "type": "pinyinAbbr", "hitCount": 120}
  ]
}
```

### 12.3 题目详情

```
GET /api/questions/{id}
```

### 12.4 统计

```
GET /api/stats
```

### 12.5 上传导入

```
POST /api/import
multipart/form-data: file=交安复习题全部.docx
```

建议改为异步：

```
POST /api/import/jobs
GET  /api/import/jobs/{jobId}
```

返回：

```json
{
  "status": "success",
  "parseReport": {
    "recognizedQuestions": 1280,
    "missingAnswer": 18,
    "answerConflict": 3,
    "answerOutOfOptions": 2,
    "caseGroups": 9,
    "knowledgeCards": 12,
    "duplicateQuestions": 35,
    "answerConsistency": {
      "verifiedAnswers": 1259,
      "reviewAnswers": 21,
      "missingAnswers": 18,
      "sampleChecked": 120,
      "sampleAnswerMatchRate": 1.0
    }
  }
}
```

### 12.6 导入预览

```
POST /api/import/preview
```

返回解析后的题目草稿，用于人工确认。

---

## 13. 部署方案（手机直接打开）

### 13.1 首选：GitHub Pages / 任意静态托管

- 本地生成 `index.html`、`questions.json`、`search-index.json`、`sw.js`、`manifest.json`；
- 上传到 GitHub 仓库，开启 GitHub Pages；
- 手机浏览器直接打开 `https://用户名.github.io/仓库名/`；
- 支持“添加到主屏幕”，像 App 一样打开；
- PWA 离线缓存索引，第二次打开更快。

### 13.2 Android APK（可选，离线安装）

使用同一份 `dist/` 静态页面打包成 APK：

```bash
# 生成静态文件后，用 Capacitor 打包
npm run build
npx cap add android
npx cap sync
npx cap open android
# 在 Android Studio 里 Build APK，或用 Gradle 命令行
cd android && ./gradlew assembleDebug
```

- 安装到 Android 手机后，打开就是独立 App；
- 不需要公网服务器，题目数据和搜索索引都在 App 内；
- 适合长期自用、发朋友、手机上不依赖浏览器；
- iOS 如需独立 App，可用本方案后用 TestFlight 或 App Store，成本较高，第一版不建议。

### 13.3 同 WiFi 局域网直接打开（调试用）

在电脑上启动静态服务器：

```bash
python -m http.server 8000
# 或
npx serve .
```

手机连同一 WiFi，浏览器访问：

```
http://电脑局域网IP:8000
```

不需要部署到公网，手机马上能打开。

### 13.4 单文件 HTML（适合分享）

- 如题目量适合，可把 `questions.json` 和 `search-index.json` 内嵌进 `index.html`；
- 生成一个单一 HTML 文件，直接发给别人；
- 通过任意静态服务器打开即可；
- 注意：手机浏览器直接打开 `file://` 本地文件体验受限，建议配合局域网 HTTP 或静态托管使用。

### 13.5 在线服务版（后续可选）

- 服务器：1 核 1G 以上，可跑 FastAPI + SQLite；
- 域名：可选；
- HTTPS：建议开启；
- Docker 部署：

```
docker build -t kaikai-search .
docker run -p 8000:8000 kaikai-search
```

---

## 14. 开发计划

### 阶段 1：解析器诊断与对齐

- 实现 docx 文本提取；
- 跑一遍 `交安复习题全部.docx`，输出统计报告：
  - 题目数、章节数、案例题组数、知识卡片数；
  - 未识别段落、缺失答案、答案冲突、答案不在选项内、缺失选项标签、重复题数；
- 人工标注 **200 道** 作为 golden set，**逐题人工核对原始答案**；
- 根据 golden set 调整解析规则；
- 目标：
  - 题目文本一致率 ≥ 99%；
  - **答案一致率 = 100%**；
  - 所有缺失/冲突答案进入人工待确认清单，不自动补写。

### 阶段 2：题库生成与静态搜索索引

- 生成结构化题库（JSON）；
- 生成 `search-index.json`：全文 + 拼音 + 首字母 + n-gram；
- 用静态搜索库（MiniSearch/FlexSearch）验证搜索效果；
- 生成 `questions.json` 等静态数据。

### 阶段 3：手机端静态页面

- 搜索页面；
- 题目列表；
- 答案复制；
- 题型/章节筛选；
- 命中高亮；
- 搜索建议/热搜词；
- 打包成可独立部署的静态文件。

### 阶段 4：手机直接打开验证

- 局域网 HTTP 打开验证；
- GitHub Pages 上线，手机浏览器直接打开；
- PWA 离线 + 搜索性能测试；
- 与源 docx 抽样比对答案一致性；
- 可选：用 Capacitor 打包 Android APK，手机直接安装。
- 部署上线。

---

## 15. 风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| Word 排版过于混乱 | 解析准确率低 | 导入预览 + 人工修正；保留原始文本 |
| **答案与题库不一致** | 严重：用户答错 | 答案三态、可溯源、冲突不静默、golden set 答案 100% 一致、人工确认后才入库 |
| 案例分析题难以解析 | 子题丢失 | 单独实现案例题组识别，背景资料整体保留 |
| 知识卡片被误判成题目 | 搜索列表混入非题 | 知识卡片单独类型，前端单独 Tab |
| 中文搜索效果一般 | 搜不到想要的题 | 全文 + 拼音 + 首字母 + n-gram 多路召回 |
| 拼音首字母无法区分同音 | 可能误召回 | 排序时全拼/原文命中优先，首字母权重低 |
| 静态版搜索卡顿 | 体验差 | Web Worker + 预计算索引 + 虚拟滚动 |
| 题库重复多 | 搜索结果重复 | 规范化去重 + 重复来源折叠 |
| 答案缺失 | 题目不完整 | 解析报告中单独统计，支持人工补录 |

---

## 16. 验收标准

### 16.1 解析与对齐

1. 导入 `交安复习题全部.docx` 后能自动生成结构化题库；
2. 能正确识别案例分析题组、背景资料、知识卡片；
3. 未识别题目能在导入报告中体现并支持人工修正；
4. 解析准确率在人工标注 golden set 上不低于 95%；
5. **答案与源文档对齐**：
   - golden set 中答案一致率必须 **100%**；
   - 缺失答案、冲突答案、答案不在选项内的问题，禁止自动生成，必须进入人工确认；
   - 任意一条入库答案都必须能回指原始段落和原始答案字符串。

### 16.2 搜索

5. 输入“安全评估”能搜到“安全风险评估”相关题；
6. 输入 `aqsc` 能搜到“安全生产”相关题；
7. 输入“安去”（错字）能通过模糊匹配搜到“安全”相关题；
8. 输入题号 `78` 或 `第78题` 能定位到对应题目；
9. 输入案例关键词“株洲高架桥”能搜到对应案例背景及子题；
10. 搜索接口响应时间 < 200ms（在线版）；
11. 静态版搜索 5000 题滑动不卡顿，索引加载 < 1s。

### 16.3 界面与手机直接打开

12. 手机浏览器直接打开 URL 即可搜索，无需安装 App；
13. 同一 WiFi 下电脑启动静态服务器后，手机可立即访问；
14. GitHub Pages 上线后，手机浏览器可直接访问；
15. 可选：打包 Android APK 后可离线安装使用；
16. 界面与参考截图风格一致，包含搜索栏、题型 Tab、题目卡片、答案卡片、复制按钮；
17. 题库更新后无需重新开发，重新导入即可。

---

## 17. 下一步

1. 已确认部署方向：**静态 PWA 版 / 单文件版，手机浏览器直接打开**；
2. 开发“解析器诊断脚本”，在 `交安复习题全部.docx` 上 dry-run，输出对齐报告；
3. 根据 dry-run 报告正式开发导入器、搜索索引和手机端静态页面；
4. 生成 `dist/` 静态文件，上传 GitHub Pages，手机浏览器直接打开；
5. 可选：用 Capacitor 将同一份静态页面打包成 Android APK。
